# R9700 capability profile and r4dx design principles

Scope: what the AMD Radeon AI PRO R9700 (gfx1201, Navi 48, RDNA4, 32 GiB, one card, HIP device 1,
Windows 11 + ROCm 7.15) can actually do, what Qwen3.8-27B actually costs on it, and the design
rules for r4dx that follow from the two. This document is the *decision layer* on top of three
research passes:

- `docs/research/r9700-arch.md` -- board specs and gfx12 ISA/builtin availability (cited below as
  **[ARCH §x]**, with the fact table at that doc's §6).
- `docs/research/r9700-microbench.md` -- on-device measurements taken 2026-09-19 (**[BENCH §x]**).
- `docs/research/r9700-kernel-lessons.md` -- libr4d kernel techniques + r4dx's own-kernel audit
  (**[LESSONS §x]**).
- `docs/perf.md` (**[PERF]**), `src/model/gemm_tuning_table.inc` (**[TUNE]**),
  `docs/container-format.md` (**[FMT]**), `docs/architecture.md` (**[ARCHDOC]**), and the engine
  source itself (`src/model/linear.cpp`, `src/model/gdn_layer.cpp`,
  `src/model/attention/include/r4dx/model/attention/linear.hpp`) for the engine's own numbers.

Every number is tagged **measured** (someone ran it on this card), **documented** (a published
spec), or **inferred** (arithmetic or reasoning by this document, from the two above). Inferred
numbers are the ones to attack with experiments -- §5 lists them.

Two rules this revision enforces throughout, because the previous revision broke both:

1. **Never mix step clocks.** [PERF]'s per-op profile is a *profiled* step (w4a16 wall 38.13 ms,
   `gpu_sum` 36.45 ms); the throughput table's steady-state step is 1/32.78 = 30.50 ms. Rescaling
   one onto the other and calling the result "measured" is not allowed. Where both appear, both are
   shown (§2.5), and the ~5.4 ms difference is itself an open question (Q2).
2. **[TUNE] is not in-model GEMM cost.** Its M=1 rows are repeat-loop benchmarks on single buffers,
   most of which are smaller than the 64 MiB MALL, and several back out *above* DRAM peak (§2.5,
   Q5). [TUNE] is a **ranking signal between kernel configurations**, never a cost model. Cost
   models in this document are byte rooflines.

---

## 1. Capability profile

### 1.1 The numbers that matter

| # | Quantity | Value | Confidence | Source |
|---|---|---|---|---|
| C1 | Compute units | 64 CU / 32 WGP (`hipInfo` reports `multiProcessorCount=32`, i.e. WGPs) | documented + measured | [ARCH §1], [BENCH intro] |
| C2 | Boost clock | 2920 MHz (base 1620, game 2350), 300 W TBP | documented | [ARCH §1] |
| C3 | VRAM capacity | 32 GiB GDDR6, 256-bit; **31.86 GiB usable** | documented + measured | [ARCH §1], [PERF] |
| C4 | VRAM bandwidth, peak | 640 GB/s | documented | [ARCH §1] |
| C5 | VRAM bandwidth, **achieved read** (2-8 GiB working set) | **604-606 GB/s = 94-95% of peak** | measured | [BENCH §1] |
| C6 | VRAM bandwidth, achieved copy (r+w) | 478-552 GB/s | measured | [BENCH §1] |
| C7 | Bandwidth at 48 MiB working set (Infinity-Cache-assisted) | 783 GB/s | measured | [BENCH §1] |
| C8 | Achieved bandwidth of the real decode GEMM (`mlp.gate_up`, 90.3 MiB, M=1) | **544-551 GB/s live/contended**; the checked-in [TUNE] row implies 641-649 GB/s, but only "**if that pre-existing measurement was itself uncontended**" ([BENCH §5]'s own words) and on a 90 MiB buffer read in a loop -- **do not use it as a bandwidth fact** | measured (live) / unverified (checked-in) | [BENCH §5], [TUNE] |
| C9 | Infinity Cache (MALL) | 64 MiB, memory-side | documented | [ARCH §1] |
| C10 | L2 / L1 / L0 | 8 MiB (measured via `hipInfo`) / 128 KiB per shader array / 32 KiB per CU | measured (L2) + documented (L1/L0, secondary source) | [ARCH §1], [BENCH intro] |
| C11 | LDS | 64 KiB/CU in CU mode, 128 KiB/WGP in WGP mode; HIP `sharedMemPerBlock = 64 KiB` either way | documented + measured | [ARCH §1], [BENCH intro] |
| C12 | LDS bandwidth, achieved | **7.83 TB/s aggregate = ~122 GB/s/CU = ~42 B/clk/CU** (contended, and the run-to-run spread is itself a clock-ramp artifact, C38; likely an underestimate, see §5 Q12) | measured | [BENCH §4] |
| C13 | Wave model | native wave32; **WMMA/SWMMAC require wave32** | documented | [ARCH §1, §2.1] |
| C14 | VGPR budget | ~1536 VGPR/SIMD in wave32 (190 VGPR -> 8 waves/SIMD naturally) | inferred from measured | [LESSONS §1.3] |
| C15 | WMMA f16 (16x16x16 -> f32) | **156.3 TFLOPS** microbench = **82% of the 191 TF/s dense peak** (C37) | measured | [BENCH §2], [ARCH §1] |
| C16 | WMMA bf16 (16x16x16 -> f32) | **161.9 TFLOPS** = 85% of dense peak | measured | [BENCH §2] |
| C17 | WMMA fp8 e4m3 (16x16x16 -> f32) | **283.9 TFLOPS** = 74% of the 383 TF/s dense peak | measured | [BENCH §2] |
| C18 | WMMA bf8 e5m2 | 276.3 TFLOPS = 72% | measured | [BENCH §2] |
| C19 | WMMA int8 `iu8` (16x16x16 -> i32) | **286.4 TOPS** = 75% of the 383 TOPS dense peak | measured | [BENCH §2] |
| C20 | WMMA int4 `iu4` K=16 | 298.2 TOPS (barely above int8 -- no real win at K=16) | measured | [BENCH §2] |
| C21 | WMMA int4 `iu4` **K=32** | **592.5 TOPS** = 77% of the 765 TOPS dense peak; 2.07x the K=16 form, 3.79x f16 | measured | [BENCH §2] |
| C22 | **fp4 / fp6 / MX-scaled WMMA** | **ABSENT.** Every `wmma_scale_*`, `wmma_*_f8f6f4`, `wmma_f32_32x16x128_f4` and every `cvt_scalef32_*` is gated `gfx1250-insts` | measured (compiler builtin table) | [ARCH §2.3] |
| C23 | SWMMAC (2:1 structured sparse) | present: 16x16x32 for f16/bf16/fp8/bf8/iu8, 16x16x64 for iu4 | measured | [ARCH §2.2] |
| C24 | `v_cvt_pk_bf16_f32` | **ABSENT** -- bf16 packing is software (2 adds + `v_perm_b32`) | measured | [ARCH §2.6], [LESSONS §1.2] |
| C25 | `global_load_tr_b64/b128` (16-bit elements) | **present** on gfx1201 | measured | [ARCH §2.7] |
| C26 | `ds_read_tr*` / `ds_load_tr*` (LDS transpose) | **ABSENT** (gfx950/gfx1250 only) | measured | [ARCH §2.3] |
| C27 | `sched_group_barrier` / `iglp_opt` | present, architecture-generic | measured | [ARCH §2.10] |
| C28 | Compile default LDS mode | `__AMDGCN_CUMODE__ = 0` (**WGP mode**); `-mcumode` flips it per TU | measured | [ARCH §5.5] |
| C29 | Kernel launch, enqueue (host) | **0.477 us** | measured | [BENCH §3a] |
| C30 | Kernel launch, completion (back-to-back on one stream) | **1.204 us** | measured | [BENCH §3a] |
| C31 | **hipGraph replay, per launch** | **0.712 us -- 41% below plain-stream** | measured | [BENCH §3b] |
| C32 | hipGraph works on Windows HIP 7.15 | **yes** -- resolves [ARCH §4]'s open question; a 1000-launch graph captured and replayed | measured | [BENCH §3b] |
| C33 | D2H 4 B + `hipStreamSynchronize` round trip (the argmax readback) | **32.9 us** | measured | [BENCH §3c] |
| C34 | `hipEventRecord` / `hipEventQuery` | 0.296 us / 0.029 us | measured | [BENCH §3d] |
| C35 | Real decode-step host enqueue, whole model | 2.48 ms (w4a16) - 4.25 ms (mxfp4) per step, ~1200 launches, against a `finish_wait` that is 93-99% of wall -- **the host is not the critical path today** | measured | [PERF] |
| C36 | PCIe | Gen5 x16 (~63 GB/s unidirectional theoretical) | documented | [ARCH §1] |
| C37 | **Dense matrix peaks** (the ceiling every WMMA number must sit under) | **191 TF/s** f16/bf16, **383 TF/s** fp8, **383 TOPS** int8, **765 TOPS** int4 (sparse doubles each; 47.8 TF/s fp32 vector x4 = 191, self-consistent) | documented | [ARCH §1] |
| C38 | **Clock/power behaviour under short kernels** | [BENCH §4]'s run1 read 3615 GB/s vs runs 2-3 at 7835-8072 GB/s, with the same early/late split inside each run's raw samples -- **a boost ramp**. Every constant here (C5, C12, C15-C21) was measured with a device-saturating kernel; a decode step is a chain of 8-150 us kernels with a host sync per step at 300 W TBP | measured (the artifact), inferred (the consequence) | [BENCH §4], §5 Q8 |
| C39 | Per-token embedding upload | `text.embed_tokens` is a **host-resident** bf16 table ([FMT], [ARCHDOC]); one row = 5120 x 2 B = **10 KiB H2D per token** from *pageable* memory (no `hipHostMalloc` anywhere in `src/core/src/model`, [ARCH §4]) -- i.e. a synchronous staging copy and a graph-hostile node (P3) | measured (the code path), inferred (the cost) | [ARCHDOC], [ARCH §4] |
| C40 | Container load | 87.79 GiB on disk (all four layouts); **15-17 s** for a quantized layout, **60 s** for bf16 | measured | [PERF] |
| C41 | Measured VRAM footprint, quantized layouts, `--max-ctx 2048` | **15.75 GiB** (was 17.79 GiB before `attn.qg/o` were quantized, a -2.04 GiB delta) | measured | [PERF] |

### 1.2 Reading the profile

Four facts dominate every design decision below.

1. **The card's arithmetic:bandwidth ratio is 474 ops/byte** (C19 286 TOPS / C5 604 GB/s;
   **inferred**). A 4-bit weight carries 2 ops per byte-and-a-bit of storage, so any GEMM whose row
   count `M` is below ~40-90 is bandwidth-bound no matter how good the kernel is (§2.4).
2. **fp4 does not exist here** (C22). MXFP4 checkpoints must be software-dequantized to fp8 before
   the WMMA, which is exactly what `r4d_gemm_mxfp4a8_nt_m64` already does. There is no future
   RDNA4 kernel that makes MXFP4 cheaper than w4a8 -- both end at an 8-bit WMMA at the same
   documented dense peak (C37, 383 for both) and near-identical measured rates (C17 284 vs C19 286).
3. **The measured WMMA rates are 74-85% of the documented dense peaks** (C15-C21 vs C37). libr4d's
   quoted "issue rates" (207 / 412 / 407 / 830 TF/s) are each **1.06-1.08x above** the hardware's
   dense peak, so they are per-wave instruction-issue extrapolations, not sustainable device
   throughput. **This document never budgets against them** (Q6 asks only how much of the 25% gap
   between 286 TOPS and the 383 TOPS peak is contention vs real).
4. **Launches are cheap individually and expensive in aggregate.** 1.204 us completion floor (C30)
   x ~1200 launches/token is 1.4 ms of pure floor, but the *measured* excess over the byte roofline
   is ~12 us per non-GEMM launch in the profiled step (§2.5) -- an order of magnitude above the
   floor, because most of r4dx's own kernels are single-workgroup scalar loops.

---

## 2. Roofline for Qwen3.8-27B on this card

### 2.1 Bytes per token

**The previous revision's 0.53125 B/param-for-everything assumption was wrong.** [FMT]'s tensor
table is explicit: only `attn.qg`, `attn.o`, `gdn.in_proj_qkv`, `gdn.out_proj`, `mlp.gate_up`,
`mlp.down` and `lm_head` have `.{layout}` variants. **`gdn.in_proj_z`, `gdn.in_proj_a/b`,
`gdn.conv1d_weight`, `attn.k`, `attn.v`, every norm and every descale are bf16/fp32-only,
regardless of `--layout`** ([FMT] lines 53-78; confirmed in code at `src/model/gdn_layer.cpp:50-60`
-- "in_proj_a/in_proj_b/in_proj_z are plain bf16 linears (never quantized)" -- and at
`src/model/attention/.../linear.hpp:1-8` for `attn.k/v`).

Per-parameter storage (**documented**, [FMT] §"Quantized layout tensors"):

| Layout | Weight | Scale | **B/param** |
|---|---|---|---|
| w4a16 | 4 bit | `uint32` per 128 K (scale + zero) | **0.53125** |
| w4a8 | 4 bit (byte-identical `wq` to w4a16) | `uint16` f16 per 128 K, no zero | **0.515625** |
| mxfp4 | 4 bit | E8M0 byte per 32 K (+ 1 B/row `wref`, negligible) | **0.53125** |
| bf16 | 16 bit | -- | **2.0** |

Parameter counts from [ARCHDOC]'s dims (hidden 5120, MLP 17408, 64 layers = 48 GDN + 16 attention,
24q/4kv x 256, GDN 16k/48v x 128, conv_dim 10240, vocab 248320) -- **inferred** arithmetic:

| Block | Quantized params | bf16-only params | Bytes/token (w4a16/mxfp4) |
|---|---|---|---|
| GDN x48 (`in_proj_qkv` 10240x5120 + `out_proj` 5120x6144 quantized; **`in_proj_z` 6144x5120**, `in_proj_a/b` 48x5120, `conv1d` 10240x4 bf16) | 4.027 G | **1.536 G** | 2.139 + **3.071** = **5.210 GB** |
| Attention x16 (`qg` 12288x5120 + `o` 5120x6144 quantized; **`k`/`v` 1024x5120 bf16**) | 1.510 G | 0.168 G | 0.802 + 0.336 = **1.138 GB** |
| MLP x64 (`gate_up` 34816x5120, `down` 5120x17408) | 17.113 G | -- | **9.092 GB** |
| `lm_head` 248320x5120 | 1.271 G | -- | **0.675 GB** |
| norms / A_log / dt_bias / descales | -- | ~0.001 G | ~0.001 GB |
| **Total** | **23.921 G** | **1.704 G** | **16.116 GB** (15.01 GiB) |

`text.embed_tokens` is a host-side gather (C39), so it does not stream from VRAM.

| Layout | Weights/token | GDN state (r+w) | KV read @ctx | Activations | **Total @ctx 2048** |
|---|---|---|---|---|---|
| w4a16 / mxfp4 | **16.116 GB** | 0.302 GB | 0.067 @2048, 1.074 @32k, 4.295 @131k | ~0.021 GB | **16.506 GB** |
| w4a8 | **15.743 GB** (-2.3%: cheaper scales) | 0.302 GB | same | ~0.021 GB | **16.133 GB** |
| bf16 | **51.248 GB** (47.73 GiB) | 0.302 GB | 0.017 @512 | ~0.021 GB | **51.59 GB** |

- GDN recurrent state = `fp32[48,128,128]` = 3.146 MB/layer, read+write, x48 = **302 MB/token**
  ([ARCHDOC] "GDN state"; **inferred** byte count).
- fp8 KV = 16 attn layers x 4 kv_heads x 256 head_dim x 2 (K+V) x 1 B = **32 KiB per token of
  context** ([ARCHDOC] "fp8 KV paging"; **inferred**).

> **VRAM reconciliation (was an unexplained ~3 GiB; now closed).** 16.116 GB of weights = **15.01
> GiB**, against C41's measured 15.75 GiB at `--max-ctx 2048` -- leaving **0.74 GiB** for the 64 MiB
> KV cache, the arena and runtime overhead. Independent check of the same accounting: quantizing
> `attn.qg/o` for 16 layers should save 1.510 G params x (2 - 0.53125) B = **2.07 GiB**; [PERF]
> measured the drop as **2.04 GiB** (C41), a 1.4% match. The old 12.68 GiB weight figure could not
> explain the measured footprint; this one can. Residual puzzle: w4a8's weights are 0.35 GiB smaller
> and [PERF] still reports 15.75 GiB for all three layouts -- one `hipMemGetInfo` breakdown at load
> settles it (Q13).

> **Finding (inferred, high confidence, needs experiment Q1): the bf16 layout does not fit.**
> 47.73 GiB of bf16 weights against a 31.86 GiB card is a **1.50x over-commit**. [PERF] reports bf16
> "uses 31.86 GiB of the R9700's 31.86 GiB" -- i.e. the driver reports saturation and Windows WDDM
> pages the remainder to host memory. Corroboration: [PERF]'s bf16 MLP block costs 439.89 ms/token
> for 34.2 GB of weights = **78 GB/s effective**, sitting between PCIe Gen5 (C36, ~63 GB/s) and
> VRAM (C5, 604 GB/s) -- consistent with ~60-80% of bf16 weight bytes arriving over PCIe. The same
> hypothesis explains [LESSONS §4]'s unexplained bf16 outlier (6.95 us/launch of host enqueue vs
> 2.1-3.5 us for the quantized layouts): WDDM does a per-launch residency/paging check over the
> kernel's resource list. **bf16 is a numerical reference layout, not a performance target.**

> **New finding (inferred, the largest single decode lever): 3.408 GB/token -- 20.6% of all bytes
> moved -- is bf16 weight that has a quantized form in every other linear but is not stored
> quantized.** `gdn.in_proj_z` alone is 62.91 MB/layer x 48 = **3.020 GB/token**, i.e. *more* than
> the entire quantized GDN weight set (2.139 GB) and 18% of the whole token. Quantizing `in_proj_z`
> plus `attn.k/v` at 0.53125 B/param removes **2.464 GB/token**, taking the token from 16.506 to
> **14.04 GB** and the decode ceiling from 36.6 to **43.0 tok/s** (§2.3). `in_proj_a/b` (0.047 GB)
> and the norms are too small to matter and feed the decay path -- leave them bf16. This is a
> converter + loader change with an accuracy question attached (Q14), not a kernel change; it is
> R1 in §4.

### 2.2 Compute per token, the 16-row padding tax, and the draft-side budget

Useful FLOPs = 2 x 25.625 G params = **51.25 GFLOP per token** (inferred).

WMMA's minimum tile is 16 rows (C15-C21 are all `16x16x*`), and at decode M=1 the GEMMs issue a
full 16-row tile per output tile [LESSONS §1.1, §1.10]. So the *issued* work is 16 x 51.25 =
**820 GFLOP/token**, of which 15/16 is multiplying by zero-padding.

At C19 (286 TOPS int8) that padded work is **2.87 ms** -- 10.5% of the 27.33 ms bandwidth floor
below. **Decode is unambiguously bandwidth-bound, and rows 2..16 of every WMMA tile are free on the
verify side.** That is half of the economic case for MTP and DFlash2. The other half -- absent from
the previous revision -- is that **the draft side is not free**:

**Draft-side byte budget** (inferred, from [FMT]'s `mtp.*` = "same tensor set as one text layer"
and §2.1's per-block bytes):

| Draft component, per draft token | Bytes | ms @604 GB/s |
|---|---|---|
| MTP layer (one GDN-style layer 108.5 MB + one MLP 142.0 MB) | 250.5 MB | 0.41 |
| **`lm_head` full vocab** (248320x5120, the single most expensive GEMM in the model) | 675.4 MB | **1.12** |
| **Total per sequentially-drafted token** | **926 MB** | **1.53** |

Drafting is autoregressive: draft row `i+1` needs row `i`'s sampled token, so a width-`w` draft from
a single-head MTP is **`w` sequential M=1 passes**, not one M=`w` pass. Therefore:

- **MTP at w=2..5 (R8)**: 3.1-7.7 ms of draft per verify step, i.e. **10-25% overhead** on a 27-30
  ms step. Viable; priced into R8's gain column.
- **DFlash2 at w=10..16 (R9) with a sequential single-head drafter: not viable.** 15-24 ms of draft
  per step doubles the step to buy at most ~2 accepted tokens -- at p=0.5 acceptance that is *worse*
  than MTP at w=4. R9 requires either (a) a **multi-head/tree drafter that emits all `w` rows in one
  pass** (then the rows really are free, §2.4), or (b) a **reduced-vocab draft head** -- a top-8k
  `lm_head` is 41.9 M params = 22.3 MB = 0.04 ms, cutting the per-draft-token cost from 1.53 ms to
  **0.45 ms** and making w=16 cost 7.2 ms. **Pick one before writing any DFlash2 code** (Q10).

**State-rollback budget** (inferred): the KV cache rolls back for free (discard slots), but GDN's
recurrent state is *updated in place*. Rejecting draft rows needs either a checkpoint of the 151 MB
fp32 state before each verify step (**302 MB r+w = 0.50 ms/step = ~1.7%** -- cheap, and the
recommended default, P10) or a verify kernel that only commits the accepted prefix. The conv state's
rolling-buffer depth must also grow with the verify width (`conv_width - 2 + max_decode_window`,
[PERF]'s review-fix item 1 -- an off-by-one here was already a blocker-class bug once).

### 2.3 Decode ceilings, per layout

At C5 (604 GB/s achieved read on a multi-GiB working set), all **inferred** from §2.1:

| Layout | Bytes/token @ctx | Ceiling ms | **Ceiling tok/s** | Measured tok/s ([PERF]) | % of ceiling |
|---|---|---|---|---|---|
| w4a16 | 16.506 GB @2048 | 27.33 | **36.6** | 32.78 | **89.6%** |
| w4a8 | 16.133 GB @2048 | 26.71 | **37.4** | 30.87 | **82.5%** |
| mxfp4 | 16.506 GB @2048 | 27.33 | **36.6** | 27.04 | **73.9%** |
| w4a16 | 17.513 GB @32k | 29.00 | 34.5 | -- | -- |
| w4a16 | 20.734 GB @131k | 34.33 | **29.1** | -- | -- |
| w4a16 **after R1** (`in_proj_z` + `k`/`v` quantized) | 14.042 GB @2048 | 23.25 | **43.0** | -- | -- |
| bf16 (if it fit) | 51.59 GB @512 | 85.4 | 11.7 | 1.41 | 12.1% (paged, §2.1) |

**Use 36.6 tok/s as the decode ceiling today** (measured bandwidth, measured bytes). At the
documented 640 GB/s peak (C4) the bound is 38.8 tok/s and is **unreachable** -- C5 is 94-95% of it on
a working set of the right size. The previous revision's 46.0 tok/s came from C8's checked-in
641-649 GB/s, which is a repeat-loop [TUNE] number on a single 90 MiB buffer; in-model that same
weight is read cold 64 times from 64 different buffers, and the live measurement of the identical
cell is 544-551 GB/s ([BENCH §5]). That figure is deleted from the ceiling derivation.

**The operational consequence is severe and new**: w4a16 is already at **89.6%** of its achievable
roofline. **No amount of kernel work on this card can exceed +12% on w4a16 decode.** The only levers
with headroom above that are (a) moving fewer bytes (R1, +17% of ceiling), (b) speculation
(R8/R9), (c) a smaller weight format, which does not exist in hardware (C22).

### 2.4 Crossover row count M* per GEMM kernel

Analytic: a 4-bit GEMM flips from bandwidth- to compute-bound when
`2 * M_eff * N * K / R = B * N * K / BW`, i.e. **`M* = B * R / (2 * BW)`** where `B` is bytes/param,
`R` is the *achieved* matrix rate and `M_eff = ceil(M/16)*16`. Achieved `R` at M=64 is backed out of
[TUNE]'s `mlp.gate_up` row (2 x 64 x 34816 x 5120 = 22.82 GFLOP per call) -- a *relative* use of
[TUNE], which is legitimate:

| Layout | WMMA type | Achieved R @M=64 (`gate_up`) | Analytic M* | Empirical M* (first M in [TUNE] more than 10% above the M=1 plateau) | Cost @M=64 vs M=1 |
|---|---|---|---|---|---|
| w4a16 | f16 (C15) | **64.0 TF/s** (356.43 us) = 34% of the 191 TF/s peak | 28 | `gate_up` ~18-24, `down` ~20, `in_proj_qkv` ~18 | **2.41x** |
| w4a8 | iu8 (C19) | **93.0 TF/s** (245.34 us) = 24% of the 383 TOPS peak | 40 | `gate_up` ~32, `down` ~40, `in_proj_qkv` ~40 | **1.68x** |
| mxfp4 | fp8 (C17) | 85.8 TF/s (265.91 us) | 38 | `gate_up` ~24, **`down` ~12**, **`in_proj_qkv` ~8-10** | 1.62x |
| bf16 | f16 | 33.0 TF/s (691.99 us, still bandwidth-limited) | n/a -- 2 B/param pushes it past 64 | none below 64 | 1.23x |

(The analytic and empirical columns disagree because the empirical one includes the 16-row rounding
and because achieved `R` is lower near the knee than at M=64. **Use the empirical column for
routing.**)

**The operational conclusion, correctly scoped**: `M* >= 16` **for w4a8 and w4a16**, so every verify
width r4dx will use (MTP 2-5, DFlash2 10-16) costs the same bytes as a single decode step on those
two layouts. From [TUNE], the whole-model quantized-GEMM sum for w4a8 is 19.10 ms at M=1 and
19.70 ms at M=16 -- **+3.1% for 16x the rows**. **mxfp4 is different**: its `down` and
`in_proj_qkv` knees sit at ~8-12 rows (`mlp.down` 50.53 us at M=1 -> 59.98 us at M=16, +18.7%), so
mxfp4 pays a real per-row cost across the whole DFlash2 band. That is an independent argument for
P1's choice of w4a8 as the verify-path layout. The bf16 `in_proj_z`/`k`/`v` GEMMs are bandwidth-bound
to M=64 and are free at every verify width either way.

### 2.5 Where the engine actually sits: gap decomposition

**Two clocks, both shown.** [PERF]'s per-op numbers come from one `Model::DecodeStepProfiled` call
(`r4dx-cli --profile`, the first generated token, hipEvent pairs around every op family): w4a16 wall
38.13 ms, `gpu_sum` **36.45 ms**. The throughput table's steady-state step is **30.50 ms**
(1/32.78 tok/s). The same offset appears in all three quantized layouts:

| Layout | `gpu_sum` of the profiled step | Steady-state step | Offset |
|---|---|---|---|
| w4a16 | 36.45 ms | 30.50 ms | +5.95 |
| w4a8 | 37.29 ms | 32.40 ms | +4.89 |
| mxfp4 | 42.23 ms | 37.00 ms | +5.23 |

A near-constant **+5.4 +/- 0.5 ms** across layouts that differ by 6 ms in step time is a signature of
*fixed* instrumentation/first-token overhead, not of a per-layout effect (**inferred**). Until Q2
re-runs the profile at a steady-state token, **use the profiled step for ranking blocks against each
other, and the steady-state step for absolute headroom.**

Block decomposition against §2.1's corrected byte counts at 604 GB/s (all rooflines **inferred**,
all ms columns **measured**, [PERF] lines 99-107):

| Block | Bytes/token | Roofline ms | Measured ms (profiled step) | Gap ms | Share of gap | % of roofline |
|---|---|---|---|---|---|---|
| MLP (64x) | 9.092 GB | 15.05 | 18.55 | **3.50** | **38.2%** | 81% |
| **GDN (48x)** | 2.139 quant + 3.071 bf16 + 0.302 state | 9.13 | 13.37 | **4.24** | **46.3%** | 68% |
| Attention (16x) | 0.802 quant + 0.336 bf16 + 0.067 KV | 1.99 | 3.21 | **1.22** | 13.3% | 62% |
| final_norm + lm_head | 0.675 GB | 1.12 | 1.25 | 0.13 | 1.4% | 89% |
| embed | ~10 KiB (C39) | ~0 | 0.07 | 0.07 | 0.8% | -- |
| **Total** | **16.506 GB** | **27.29** | **36.45** | **9.16** | 100% | 75% |

Against the **steady-state** 30.50 ms the same total gap is only **3.2 ms**, which is the number
that actually bounds the roadmap (§2.3).

What the corrected table says, and what changed:

- **The MLP is not done.** At 81% of its own stream roofline there is **3.5 ms** in the profiled step
  (and a proportional share of the 3.2 ms steady-state gap) sitting in the MLP block. The previous
  revision's "97% of roofline, stop optimizing MLP" was an artifact of rescaling percentages onto
  the wrong step time.
- **GDN is 46% of the gap, not 80%** -- and the reason is *launch count*, not a pathological kernel:
  48 layers x 11 launches is 40% of the token's kernels. Normalised, every block shows the same
  thing: **GDN 88 us/layer, MLP 55 us/layer, attention 76 us/layer of excess**, which across the
  ~770 non-GEMM launches per token is **~12 us each against a 1.2 us completion floor (C30)**.
  [LESSONS §2] explains why: all nine of r4dx's own kernels are plain scalar grid-stride loops with
  no vector loads, no packed converts, and `grid = dim3(rows)` -- which at decode M=1 means **one
  workgroup for the whole kernel**, using 1 of 64 CUs.
- **The biggest single line in the GDN roofline is `gdn.in_proj_z`**: 3.071 GB of the block's
  5.512 GB, i.e. **5.08 ms of the 9.13 ms roofline**, on a bf16 GEMM that (a) has no quantized form
  in the container (R1) and (b) is launched with a hardcoded `WV=4,SK=4,MB=1`
  (`gdn_layer.cpp:59-60`) that `tune_gemm.py` has never swept (R4). Fusing GDN's small kernels (R6)
  cannot touch either.

**Launch census, re-derived from source** (replaces [LESSONS §4]'s stale 1204, which was counted
before `attn.qg/o` were quantized):

| Kind | Count/token | Derivation |
|---|---|---|
| GEMMs | **433** | GDN 5x48 (`qkv,a,b,z,out`) + attn 4x16 (`qg,k,v,o`) + MLP 2x64 + `lm_head` |
| -- of which quantized | **257** | GDN 2x48 + attn 2x16 + MLP 2x64 + 1 |
| -- of which bf16 | **176** | GDN `a,b,z` 3x48 + attn `k,v` 2x16 |
| **quant/cast launches** | **257** | `ApplyLinear` issues exactly one `cast_bf16_to_f16` / `QuantActI8` / `quant_act_fp8e4m3_row` per quantized GEMM (`src/model/linear.cpp:107-128`) -- **not 224**, and it is the quantized-GEMM count, not the GEMM count |
| norm / elementwise / state / attention kernels | ~510 | GDN 4x48 (rmsnorm, conv_update, recurrent_update incl. fused gated norm, residual_add) + attn ~9x16 + MLP 3x64 + 6 |
| **Total** | **~1200** | matches [LESSONS §4]'s 1204 by coincidence of two offsetting changes |

Note for P2: **there are no byte-identical duplicate quantizations to eliminate.** Each of GDN's
`in_proj_z/a/b` and attention's `k/v` is bf16 and therefore never quantizes its input, so
`x_normed` is quantized exactly once per block. A "quantize-once-per-producer-row" pass would remove
zero launches; fusing into the producer (P2) is the only available win.

**Per-layout isolation of the quant/cast kernel** -- done against the **MLP block's own byte
roofline** rather than against [TUNE] sums, because [TUNE] cannot be used as in-model cost (rule 2
above). MLP per layer = 142.05 MB (w4a16/mxfp4) / 137.86 MB (w4a8) of weights, 2 GEMMs, 2 quant/cast
calls, 1 silu_mul, 1 residual, 1 rmsnorm:

| Layout | MLP measured/layer | Roofline/layer | Excess/layer | Excess per quant/cast call (upper bound) | Scaled to 257 calls |
|---|---|---|---|---|---|
| w4a16 (`cast_bf16_to_f16`, own, scalar) | 289.8 us | 235.2 us | 54.6 us | **27.3 us** | 7.0 ms |
| w4a8 (`r4d_quant_act_i8`, libr4d, vectorized) | 298.6 us | 228.2 us | 70.4 us | **35.2 us** | 9.0 ms |
| mxfp4 (`QuantActFp8Kernel`, own, scalar, `grid=dim3(M)`) | 346.1 us | 235.2 us | 110.9 us | **55.5 us** | 14.3 ms |

These are **upper bounds** (the excess also contains whatever the GEMMs themselves lose to the
roofline, and the silu/residual/norm launches), and the scaled column exceeds the steady-state
headroom, so treat the *ratios* as the finding: **mxfp4's quant kernel costs ~2x w4a16's cast per
call, and the mxfp4-vs-w4a16 whole-step delta this predicts (28.2 us x 257 = 7.2 ms, partly offset
by mxfp4's cheaper GEMMs) matches the measured `gpu_sum` delta of 5.78 ms.** The previous revision's
"7.7 ms indictment" reached a similar place by subtracting [TUNE] sums from measured steps, which is
invalid twice over: it charged 176 bf16 GEMMs (~6 ms/token of real streaming) into the "non-GEMM
remainder", and its [TUNE] baselines are cache-flattered by *different* amounts per layout (mxfp4's
entire 1.2 ms [TUNE] advantage over w4a16 is `mlp.down` and `gdn.in_proj_qkv`, precisely the two
rows that back out at 937 and 915 GB/s -- above DRAM peak; Q5). The honest statement of the mxfp4
deficit is: **candidate causes are (a) r4dx's scalar fp8 quant kernel and (b) mxfp4 [TUNE] rows
being ~1.5x cache-flattered on `down`/`qkv`; Q4 and Q5 separate them.** libr4d says the same thing
verbatim at `r4d_gemm_mxfp4a8_nt_m64.hip:81-84`.

### 2.6 Prefill ceiling

Prefill streams the same 16.116 GB of weights (w4a16; 15.743 GB w4a8) once per chunk of `T` rows,
and does `51.25 GFLOP x T` of work.

- **Bandwidth time/token** = 26.68 ms / T (w4a16), 26.06 ms / T (w4a8).
- **Compute time/token** = 51.25 GFLOP / R_eff. At R_eff = 186 TOPS (65% of C19's **measured** 286;
  the libr4d issue rate is not used, §1.2 fact 3) that is **275 us**.
- **Chunk crossover `T*` = 26680 us / 275 us ~= 97 rows** (inferred).

| Regime | Ceiling |
|---|---|
| T=64 (today), bandwidth-bound | 417 us/token -> **2398 tok/s** (w4a8: 407 us -> 2457) |
| T=256, compute-bound at 186 TOPS | 275 us/token -> **3636 tok/s** (optimistic, see below) |
| Measured today (w4a8 606 / w4a16 613 tok/s) | 1650 us/token -> **25% of the T=64 ceiling** |

Decomposing that 1650 us: quantized GEMMs at M=64 are ~486 us/token ([TUNE], relative use only),
the bf16 `z`/`k`/`v` GEMMs ~100 us/token (roofline), so **~590 us/token (36%) is GEMM and
~1060 us/token (64%) is the GDN `kkt_solve`/`chunk_scan` pair, the attention prefill kernel, the
unfused elementwise kernels and launch overhead -- none of which is currently profiled** (Q7).

**Conclusion, corrected**: a larger chunk only lowers the *weight-streaming* term, from 417 to
104 us/token at T=256. It does nothing for the 64% that dominates, and the attention-prefill term
per token actually **grows** with chunk length (O(T) work per row within the chunk), so the T=256
"compute ceiling" above is optimistic. So: **the chunk cap and the prefill GEMM kernel (R10) buy
at most the ~710 tok/s the roadmap states; the dominant prefill cost is unmeasured, and R11 (profile
and fix the non-GEMM path) outranks it.**

---

## 3. Design principles for r4dx

Each principle names the fact that forces it.

### P1. Default weight layout is **w4a8**; route by M, never by prefill/decode

**Fact**: w4a8 stores the byte-identical `wq` as w4a16 with a 2-byte-per-128 scale instead of 4
([FMT]), so it moves **2.3% fewer bytes per token than w4a16/mxfp4** (15.743 vs 16.116 GB) -- worth
0.62 ms/token = ~+0.7 tok/s for free. Above M*, int8 WMMA (C19 286 TOPS measured, 383 documented
peak) is the fastest path the hardware offers for a 4-bit checkpoint, and [TUNE] shows w4a8 at
**1.45x** w4a16 on `mlp.gate_up` @M=64 (245.34 vs 356.43 us) and **1.81x** on `mlp.down` (119.41 vs
216.56 us), while already being marginally *faster* at M=1 on every shape. And mxfp4's M* of ~8-12
on two shapes (§2.4) makes it the wrong layout for any verify width above ~8.

- **w4a8 is the default container layout.** w4a16 becomes the accuracy-comparison layout, mxfp4 the
  checkpoint-compatibility layout, bf16 the numerical reference (which does not fit, §2.1).
- Routing inside a layout is by `M` band only, through the existing `PickTuning` table. The bands
  that matter: `M in [1]` decode, `M in [2,5]` MTP verify, `M in [6,16]` DFlash2 verify, `M in
  [17,64]` prefill chunk. All four are below or near M* for w4a8 (§2.4), so **all four are the same
  bandwidth-bound kernel** -- the table only picks `WV/SK/NPW`.
- Do **not** add a per-M layout switch (e.g. "w4a16 at decode, w4a8 at verify"): that would need two
  copies of ~15 GiB of weights.
- **The bf16-only linears are part of the layout decision too.** `in_proj_z`/`k`/`v` are 20.6% of the
  token in every layout (§2.1) and are *not* covered by `--layout`. Quantizing them (R1) is worth
  more than every kernel item in §4 combined.

### P2. Fuse activation quantization into its producer (norm, silu, gated-norm)

**Fact**: **257** quant/cast launches per decode token (`linear.cpp:107-128`, one per quantized GEMM
-- §2.5's census), each costing up to 27/35/55 us (w4a16/w4a8/mxfp4) against kernels that move
10-35 KB; r4dx's `QuantActFp8Kernel` launches `grid = dim3(M)` = **one workgroup** at decode
[LESSONS §2].

- Every producer of a GEMM input already computes a per-row reduction (`RmsNormKernel`,
  `ResidualRmsNormKernel`, the GDN gated norm) or already touches every element (`SiluMulKernel`).
  The absmax pass is free inside them.
- **There is no duplicate-quantization win** (§2.5): each normed row feeds exactly one quantized
  GEMM, because `in_proj_z/a/b` and `k/v` are bf16. Fusion is the only lever here.
- **int8 path**: the fused epilogue must emit bytes **already in `fragA8` WMMA-fragment order**, not
  row-major -- [LESSONS §1.11] measured the strided alternative at 106.7 us vs 96.5 us at M=64.
- **fp8 path**: plain row-major e4m3 with `scale = max(1e-8, absmax)/448`, **no** fragment reorder --
  `r4d_gemm_mxfp4a8_nt_m64` reads A row-major (`A + r*K + kg + mhalf`) [LESSONS §1.11].
- While in there, fix the scalar loads: use `ushort4`/`uint4` 16-byte loads and `f2bf2`/`f2bf4`
  packed converts (2 adds + one `v_perm_b32`, C24) the way every libr4d kernel does
  [LESSONS §1.2, §2].
- **`r4dx_residual_rmsnorm_bf16` already exists and is never called** [LESSONS §3, §5.1]. Wiring it
  into the two layer boundaries removes 128 launches/token for zero new kernel code.

### P3. Capture the decode step and the verify step as HIP graphs -- after making the step device-resident

**Fact**: graph replay is 0.712 us/launch vs 1.204 us (C30/C31), **and hipGraph demonstrably works
on this Windows stack** (C32 -- this resolves [ARCH §4]'s open question). At ~1200 launches that is
a **0.59 ms/token** GPU-side saving. It is *not* also a host-enqueue saving: `host_enqueue` is
2.48-4.25 ms against a `finish_wait` of 93.5% of wall (C35), i.e. **the host issues and then
blocks -- it is not the critical path today** ([BENCH §3b] independently estimates graphs at ~1% of
a decode step). Revisit that when the step reaches ~20 ms.

- Decode is otherwise the ideal graph: fixed shapes, fixed arena addresses (`Arena::Reset()` is a
  host-side offset rewind, so device pointers repeat exactly), fixed control arrays
  (`GdnControlCache` already uploads each `(T,slot)` once, ever [PERF]).
- **Three things break the capture, and all three must be fixed first:**
  1. **The per-token embedding upload** (C39): a 10 KiB H2D from *pageable* host memory every step.
     Either move `text.embed_tokens` into VRAM and gather on-device (2.543 GB bf16; 15.01 weights +
     2.37 embed + 4.0 KV@131k + ~0.7 arena = **22.1 of 31.86 GiB, still fits**) or pin a staging
     buffer. The VRAM option is strictly better: it makes the whole decode graph device-resident,
     which is what this principle wants anyway.
  2. **The argmax D2H** (C33, 32.9 us): 0.11% of a 30.5 ms step today, but a captured graph cannot
     contain a host decision. Sampling must become device-side -- and **non-greedy sampling is not
     designed at all yet**; under speculation it becomes a per-verify-row accept/reject decision,
     which must also be device-side or the graph is pointless.
  3. The token id / position / sequence length, which must be device-resident (they nearly are) or
     updated via `hipGraphExecKernelNodeSetParams`.
- Capture **after** P2, not before: fusing first removes ~250 nodes from the graph.
- Capture the verify step separately, one graph per supported verify width.

### P4. fp8 KV is a bandwidth decision, not a capacity decision -- and it must stay fp8 at long context

**Fact**: KV is 32 KiB per token of context (§2.1). At 131072 tokens that is 4.295 GB read per decode
step = **20.7% of all bytes moved**, dropping the ceiling from 36.6 to 29.1 tok/s (§2.3). bf16 KV
would make it 8.59 GB = 34% and 23.6 tok/s.

- Capacity is not the constraint: 15.01 GiB weights + 4.0 GiB fp8 KV @131k + ~0.7 GiB arena = **19.7
  of 31.86 GiB** (the corrected weight figure happens to land in the same place as the old one).
  **The full 131072-token context fits with ~12 GiB to spare** -- enough to also hold the embedding
  table in VRAM (P3) and still have room.
- Therefore the reason to keep fp8 KV is the ~21% of decode throughput it buys at long context, and
  any future "bigger KV" idea (bf16 KV for accuracy, larger head_dim) must be priced against that,
  not against VRAM.
- A 4-bit KV would buy a further 10% at 131k. Not worth the accuracy risk until 131k is a real
  workload (Q17).

### P5. Non-temporal weight loads are a per-(shape, M) tuning entry, not a standing principle

**Fact, corrected**: nothing in a decode step is re-read *within* the step. Each weight byte is read
once, each KV byte is read once by its one attention layer, and the 3.146 MB/layer GDN state is read
and written by exactly one kernel (`r4d_gdn_recurrent_update`, which `gdn_layer.cpp:124-129` shows
also absorbs the gated norm). Reuse of KV and GDN state is *across steps*, and between two touches
16.5 GB streams through a 64 MiB MALL -- **256x the cache** -- so no temporal hint can preserve that
residency. The previous revision's justification ("NT protects the cache for KV and GDN state") and
its corollary ("order GDN kernels so the state stays cache-resident") were protecting reuse that
does not exist.

- The fact that actually supports NT is libr4d's **measured per-shape behaviour**: +3-4% at M=8/16,
  and a measured **loss** from M=24 up on long-K shapes (`r4d_gemm_w4a16_nt_m64.hip:77-84`). That
  makes `NT` a per-(shape, M) table entry -- i.e. exactly R12 -- not a global setting. Every [TUNE]
  row currently carries `NT=1` because the sweep never varied it.
- The real argument for ordering the GDN chain is **R6's fused kernel keeping state in registers/LDS
  across the conv/recurrent/gated-norm steps**, which is a different claim from cache residency and
  should be argued on its own terms.
- **MALL write behaviour is unexamined and is a genuine R6 design input**: the step writes 151 MB of
  GDN state plus all activations *through* a memory-side cache. Whether those stores allocate in
  MALL, and whether gfx12 exposes a store temporal hint worth using, is not covered by any of the
  three research docs (Q15).
- Corollary for benchmarking (unchanged and now load-bearing): [TUNE]'s absolute microseconds for
  shapes under ~64 MiB are **Infinity-Cache-flattered** -- mxfp4 `mlp.down` at 50.53 us implies
  937 GB/s and `gdn.in_proj_qkv` at 30.44 us implies 915 GB/s, both above DRAM peak (C4) and above
  measured stream (C5), exactly as `r4d_gemm_mxfp4a8_nt_m64.hip:81-84` warns. **Ranking signal only,
  never a cost model** (Q5).

### P6. LDS budget, CU mode, and occupancy for r4dx-owned kernels

**Facts**: 64 KiB per block either mode (C11); WGP is the compile default (C28); LDS delivers
7.83 TB/s aggregate (C12); a 16x16x16 iu8 WMMA consumes 512 B of A+B operands per 8192 ops, so
**peak iu8 (C19) demands 17.9 TB/s of operand bytes** (inferred) -- **2.3x more than LDS can
supply**; `kkt_solve` at 190 VGPR runs 8 waves/SIMD naturally and **forcing 10 waves is 1.57x
slower** (39.9 vs 25.4 us) [LESSONS §1.3].

- **Register reuse must be >= 2.3x for any WMMA kernel we write** (that is what the 17.9-vs-7.83
  TB/s arithmetic actually requires); **design for 4x** as the margin, since C12 is a contended
  number that could move either way (Q12). 4x reuse means a **4x4 fragment register tile per wave**
  (64x64 output), costing 4x4x8 = 128 accumulator VGPRs + ~60 for operands/addressing ~= 190 VGPR
  -> **8 waves/SIMD** (C14). A 2x2 wave tile (32 accumulator VGPRs) gives only ~2x reuse -- below
  the requirement.
- **LDS budget target <= 32 KiB per block** so the default WGP mode (C28) keeps 2 blocks resident
  per WGP. Use `-mcumode` **per translation unit only** (the libr4d pattern, [ARCH §5.4]) and only
  for a kernel whose LDS footprint exceeds 32 KiB and that wants the CU's LDS bandwidth to itself --
  `r4d_gdn_chunk_scan` at ~61 KB is the one existing example.
- **Never set a `__launch_bounds__` minimum-waves above the compiler's natural VGPR-driven
  occupancy.** Measure, do not assume.
- For bandwidth-bound kernels (every decode GEMM, every fused elementwise kernel), the occupancy
  target is "enough loads in flight to cover DRAM latency": ~483 KB in flight at 604 GB/s x 800 ns
  (**inferred**, latency assumed), i.e. **>= 4 waves/SIMD with 16 B/lane loads, or fewer waves each
  with 4+ outstanding vector loads.** The current single-workgroup elementwise kernels (§2.5) miss
  this by ~64x.
- Replace `__syncthreads()` with a workgroup-scoped `lds_barrier()` wherever the barrier only orders
  LDS: on gfx12 `__syncthreads()` emits `global_inv scope:SCOPE_SE`, throwing away L0/L1 lines the
  kernel is about to reuse [LESSONS §1.6].

### P7. Idioms to use from libr4d

- **WMMA fragment layout `idx = lane % 16, k = 8*(e>>2) + 4*(lane>>4) + (e&3)`** is load-bearing
  everywhere; swapping WMMA operands transposes the result for free [LESSONS §1.1]. Keep all weight
  permutation **offline** (the container already does it) -- measured 1.15x at M=8 to 1.44x at M=64
  over reading checkpoint order.
- **`global_load_tr_b128`** (C25) only helps 16-bit operands needing a *runtime* transpose -- i.e.
  the attention V-fetch, which already uses it. There is no 4-bit or 8-bit transpose load and no LDS
  transpose at all (C26), so anything else must be permuted offline.
- **bf16 packing**: `f2bf2` = 2 adds + `v_perm_b32` (C24). The truncating `f2bf2t` variant is
  1.5-2.2% faster but moves the fp64-oracle error from 3.30e-3 to 9.02e-3/1.17e-2 -- **use it only
  for intermediates that do not feed a recurrence** [LESSONS §1.2]. Prefer f16 where numerically
  acceptable: `v_cvt_pkrtz_f16_f32` does two converts in one instruction.
- **`sched_group_barrier` coarsely**: group `(global load, LDS read, WMMA)` over 2-4 k-steps, not
  per step -- "the group barrier PINS the order at every boundary" [LESSONS §1.6]. Without it, LLVM
  single-buffers LDS and drains before every WMMA.
- **Split-K is not determinism-free**: a different `SK` changes the LDS reduction order and has
  already flipped generated tokens on the real model [PERF]. Pin `SK` per shipped tuning table and
  treat a retune as a behaviour change.

### P8. What NOT to build

| Do not build | Because |
|---|---|
| Any fp4 / fp6 / MX-scaled WMMA path | It does not exist on gfx1201 (C22). MXFP4 must dequantize to fp8 first, which `r4d_gemm_mxfp4a8_nt_m64` already does optimally (folded-exponent table, e4m3 subnormals honoured, [LESSONS §1.7]). |
| mxfp4 as the default layout | Same weight bytes as w4a16 (2.3% more than w4a8), same 8-bit WMMA rate (C17 vs C19), an M* of ~8-12 on two shapes (§2.4), and currently 27% slower end to end. Keep for compatibility only. |
| A **sequential** wide drafter for DFlash2 | §2.2: w sequential M=1 draft passes cost w x 1.53 ms, which at w=16 exceeds the whole verify step. Wide speculation needs a one-pass multi-head/tree drafter or a reduced-vocab draft head. |
| SWMMAC / structured sparsity (C23) | Requires 2:1-structured-sparse weights. This checkpoint is dense; making it sparse is a model-quality project, not a kernel project. |
| `iu4 x iu4` WMMA (C21, 592 TOPS) at decode | Decode is bandwidth-bound at M=1 (§2.2) -- 2x the matrix rate buys nothing. Only revisit **after** a prefill kernel exists and is measured compute-bound, and only with int4-activation accuracy evidence (Q11). Note C20: at K=16, iu4 is only 4% faster than iu8, so the win requires the K=32 form specifically. |
| P2P / multi-GPU | GPU RULE: device 1 only. Windows HIP lacks working IPC/P2P for this pattern, and at 4-bit the whole model plus a 131k KV cache fits in 19.7 of 31.86 GiB (P4). |
| Fine-grained / uncached device memory | Those allocations are GPU-uncached [ARCH §3]; on a 16.5 GB/token streaming workload that is catastrophic. |
| bf16-layout performance work | It over-commits VRAM by 1.50x and is being paged over PCIe (§2.1). Fixing its throughput means shrinking it, which is what the 4-bit layouts already are. |
| Cooperative-groups grid sync | Unknown Windows support [ARCH §4] and unnecessary: every kernel here is either embarrassingly parallel or per-layer serial. |
| Any cost model built on [TUNE] absolute microseconds | Rule 2 at the top of this document; §2.5; Q5. |

### P9. Prefill kernel specification

Only build this **after** Q7 (prefill per-op profile) shows the GEMM share is worth it (§2.6 says it
is ~36% today). When built:

- **Premise inversion.** Every skinny kernel assumes "the weight is read once and never reused, so
  never stage it in LDS" [LESSONS §1.10]. At T >= 128 a dequantized 16x16 weight tile feeds `T/16`
  row-tiles. The prefill kernel is a classically tiled GEMM, not a wider skinny kernel.
- **Block tile 128 (M) x 256 (N), K-step 64**, 8 wave32 waves per block arranged 2x4, each wave
  owning a **4x4 fragment register tile** (= 64x64 output, P6) -> 2x4 waves x 64x64 = 128x256 =
  exactly the block tile, at 8 waves/SIMD. (The previous revision specified a 128x128 block with the
  same 2x4 waves of 4x4 fragments, which tiles 32768 outputs into a 16384-output block -- arithmetically
  impossible. The alternative resolution, 128x128 with **four** waves of 4x4, is also legal and
  halves the LDS; it loses the 8-wave occupancy sweet spot, so it is the fallback, not the default.)
- **WMMA type per layout**: `iu8` for w4a8 (C19), `fp8_fp8` for mxfp4 (C17), `f16` for w4a16 (C15).
  The checkpoint format decides; fp8 and iu8 are the same speed. Reuse the existing dequant paths
  verbatim -- `dequant8()` (two ANDs + a shift) and `r4d_mxfp4_unpack8()` (folded-exponent table),
  both measured under a few percent of kernel time even at M=64 [LESSONS §1.7, §1.8].
- **LDS, recomputed for 128x256**: A-tile 128x64 int8 = 8 KiB, B-tile 256x64 int8 = 16 KiB,
  **double-buffered = 48 KiB** -- which exceeds the <= 32 KiB / 2-blocks-per-WGP target (P6), so
  either (a) stage B **raw 4-bit** and dequantize in registers: 8 + 8 = 16 KiB, **32 KiB
  double-buffered**, meeting the target exactly -- **this is the default** -- or (b) accept 48 KiB
  and one block per WGP, which needs measuring before it is chosen.
- **Double-buffering constraints specific to gfx12**: there is no direct-to-LDS / async copy and no
  `ds_read_tr` (C26), so the prefetch is an explicit `global_load -> VGPR -> ds_write` for buffer B
  while WMMAs consume buffer A. LLVM will single-buffer this unless told otherwise -- coarse
  `sched_group_barrier` groups over 2-4 k-steps (P7), a hard `sched_barrier()` between the load
  group and the WMMA group (without it, prefetch is inert [LESSONS §1.6]), and `lds_barrier()`
  rather than `__syncthreads()` (P6).
- **Drop split-K.** `SK` exists because "the independent N fragments do not fill the part" at small
  M. At T >= 128, `(N/256) x (T/128)` tiles already saturate 64 CUs, and dropping `SK` also removes
  the rounding-order-changes-output hazard (P7).
- **Expected throughput**: LDS supplies 7.83 TB/s (C12) against 17.9 TB/s of peak iu8 operand
  demand, so 4x register reuse makes the ceiling operand-feasible. Target **170-215 TOPS**, which is
  **44-56% of the 383 TOPS documented dense peak (C37)** and 60-75% of the 286 TOPS measured (C19).
  At 186 TOPS that is 51.25 GFLOP/token -> **275 us/token**, versus ~590 us/token of GEMM time
  today (§2.6): **~2.1x on the GEMM share**, which is ~36% of prefill.
- **Occupancy**: start at natural occupancy, no `__launch_bounds__` minimum (P6). Measure before
  forcing anything.

### P10. Speculation must budget the draft side and the recurrent state, not just the verify rows

**Fact**: §2.2. Rows 2..16 of a WMMA tile are free; *producing* those rows is not, and GDN's state
does not roll back for free.

- **Every speculation design must state three numbers before it is approved**: (1) draft bytes per
  step (`w` x 926 MB for a sequential full-vocab MTP drafter, or `w` x 273 MB with a top-8k draft
  head), (2) the state-rollback cost (0.50 ms/step for the 151 MB fp32 checkpoint, or "verify writes
  only the accepted prefix"), (3) the required acceptance rate for the whole thing to beat
  `1 / 30.5 ms`.
- **Default rollback strategy: checkpoint.** 302 MB r+w = 0.50 ms/step (~1.7%) is cheap, obviously
  correct, and independent of the verify kernel's internals. Only replace it with accepted-prefix
  commit if a measurement says the 1.7% matters.
- **The conv-state rolling buffer must be sized `conv_width - 2 + max_decode_window` with
  `max_decode_window` = the maximum verify width** ([PERF] review-fix item 1). This was already a
  blocker-class off-by-one once; widening speculation re-opens it.
- The accept/reject decision itself must be **device-side** (P3), or every verify step pays a
  32.9 us host round trip per row (C33).

---

## 4. Re-ranked roadmap, Milestone 3+

Baselines ([PERF], measured): w4a16 30.50 ms/token (32.78 tok/s), w4a8 32.40 ms (30.87), mxfp4
37.00 ms (27.04), prefill 606-613 tok/s. **Ceilings (§2.3): 36.6 / 37.4 / 36.6 tok/s, i.e. the
steady-state headroom is 3.2 / 5.7 / 9.7 ms per token.** Every kernel-side gain below is capped by
those numbers; only R1 raises the cap. All gains **inferred** unless marked measured.

| # | Item | Expected gain | Effort | Depends on | Justified by |
|---|---|---|---|---|---|
| **R1** | **Quantize `gdn.in_proj_z` and `attn.k`/`attn.v` in the converter + loader** (they have no `.{layout}` form today) | **-2.464 GB/token = -4.08 ms at roofline**; ceiling 36.6 -> **43.0 tok/s**, realistic **+4 to +5 tok/s** | **M** (converter, loader, A/B accuracy) | Q14 | §2.1, §2.5 (`in_proj_z` is 5.08 ms of GDN's 9.13 ms roofline) |
| **R2** | Fuse act-quant into rmsnorm / silu / gated-norm epilogues; vectorize loads and use packed converts in all of `src/kernels` | mxfp4 **-6 to -9 ms** (+5 to +8 tok/s), w4a8 **-3 to -4 ms** (+3 to +4), w4a16 **-2 to -3 ms** (+2 to +3); 257 fewer launches. **Capped by the headroom above.** | **M** | R3 | §2.5 (27/35/55 us per quant call), P2 |
| **R3** | Call the already-written `r4dx_residual_rmsnorm_bf16` at both layer boundaries | -0.15 ms (pure GPU dispatch floor, 128 x 1.204 us) to **-0.5 ms** (if the measured ~12 us/small-kernel cost holds at steady state); **+0.2 to +0.6 tok/s**, 128 fewer launches | **S** (zero new kernel code) | -- | [LESSONS §3, §5.1], C30 |
| **R4** | Extend `tune_gemm.py` to the **bf16** shapes (`in_proj_z` 6144x5120, `k`/`v` 1024x5120): today `z` runs a hardcoded `WV=4,SK=4,MB=1` (`gdn_layer.cpp:59`) and `k`/`v` a validity-only heuristic (`attention/linear.hpp:30-41`), on 3.4 GB/token | 5% on 3.4 GB/token = **-0.28 ms**; more if the fallback is badly chosen | **S** | -- | §2.5, source as cited |
| **R5** | Profiling truth: re-run `DecodeStepProfiled` at a **steady-state** token, reconcile `gpu_sum + host_enqueue` against 1/tok_s, add per-kernel hipEvent pairs inside `GdnLayer::Forward` / `AttentionLayer::Forward`, extend to prefill, and sample `rocm-smi` clock/power during the run | 0 directly; **unblocks R6, R10, R11 and settles Q2/Q3/Q8** | **S** | -- | §2.5's 5.4 ms unexplained offset, C38 |
| **R6** | Fused GDN decode kernel: `conv_update + recurrent_update(+gated norm) + act-quant` in one launch, state in registers/LDS across the chain | **-0.5 to -1.5 ms/token -> +0.5 to +1.5 tok/s** (down from the previous revision's -3 to -4.5: GDN's non-GEMM excess is 4.24 ms in the *profiled* step, ~1.4 ms scaled to steady state) | **L** | R5, R2 | §2.5, P5 |
| **R7** | HIP-graph-capture the decode step, with the embedding table moved to VRAM and sampling made device-side | **-0.59 ms (~1200 x 0.49 us) -> +0.6 tok/s**; proportionally more once the step is ~20 ms. Not a host-enqueue win (C35). | **M** | R2 (fewer nodes) | C31/C32, P3 |
| **R8** | **MTP self-speculation, 2-5 verify rows**, draft cost priced in (w x 1.53 ms), GDN routed to `chunk_scan`, 151 MB state checkpointed per step | at w=4, p=0.6: step 30.5 + 6.1 draft + 0.5 rollback = 37.1 ms for ~2.3 tokens -> **~62 tok/s (1.9x)**; at p=0.5, ~50 tok/s (1.5x) | **L** | R2, R7, Q9, P10 | §2.2, §2.4 |
| **R9** | **DFlash2, 10-16 verify rows** -- **only** with a one-pass multi-head/tree drafter or a reduced-vocab draft head | with a 273 MB/row drafter at p=0.5: **~2.5-3x**; with a sequential full-vocab drafter: **worse than R8** | **L+** | R8, Q10, P8 | §2.2's draft budget |
| **R10** | Prefill: raise the chunk cap above 64 via a tiled prefill GEMM kernel (P9) | GEMM share ~590 -> 275 us/token; **prefill 606 -> ~710 tok/s alone** | **L** | R5 (profile first!) | §2.6, P9 |
| **R11** | Prefill non-GEMM path: profile and fix the ~1060 us/token (64%) in GDN chunk-scan / attention prefill / elementwise | unknown until R5; potentially **2x prefill**. **Outranks R10.** | **M-L** | R5 | §2.6 |
| **R12** | Sweep `NT` per (shape, M) in `tools/profile/tune_gemm.py` (held fixed at 1, never swept), **after** Q5's cache-flattery fix lands in the sweep harness | +/-2 to 4% on GEMM time | **S** | Q5 | P5, [LESSONS §1.4] |
| **R13** | Long-context validation: measure decode at 32k and 131k; confirm the §2.3 KV curve; add a `hipMemGetInfo` VRAM breakdown at load (Q13) | 0; de-risks the product claim and closes the VRAM accounting | **S** | -- | §2.1, §2.3, P4 |
| **R14** | Demote bf16: warn at load that it over-commits VRAM; stop treating its tok/s as a metric | 0; removes a misleading number from every table | **S** | Q1 | §2.1 |

**Ordering rationale.** **R1 is first because it is the only item that raises the ceiling** -- every
other decode item competes for 3.2 ms (w4a16) of headroom, while R1 removes 4.08 ms of *floor*.
R2-R4 are cheap and are prerequisites for everything expensive. R5 must precede R6/R10/R11 because
the profiled step and the steady-state step currently disagree by 5.4 ms, which is larger than R6's
entire gain. R8/R9 are the largest wins of any kind, because §2.2 shows 15/16 of the verify-side
matrix work is already being thrown away -- but they are worth nothing until the per-step cost is
down (R2/R6), the verify step can be replayed cheaply (R7), and the draft side is budgeted (P10).
Prefill (R10/R11) ranks below all of that unless long prompts become the dominant workload, and
**R11 outranks R10** (§2.6).

Post-R1..R7 projection (**inferred**, compounding conservatively): w4a16 30.50 -> **~25-26
ms/token = 38-40 tok/s** against R1's new 43.0 tok/s ceiling (88-93% of it). **Beyond that point the
only remaining levers on this card are speculation (R8/R9) or a smaller weight format, and no
smaller weight format exists in hardware (C22).**

> **R1: DONE (2026-09-20, measured).** `gdn.in_proj_z`/`attn.k`/`attn.v` quantized in the converter
> and loader (docs/status.md's own R1 section has the full converter/loader/kernel-routing writeup);
> Q14's accuracy gate held (no tensor's error crossed the ">2x worse" bar against the existing
> quantized linears -- docs/status.md's table). Real container reconverted
> (`D:\models\r4dx\qwen38-27b-v3.r4dx`, 45.02 GiB, was 87.79 GiB). **Measured `--mtp 0` decode**:
> w4a16 32.83 -> **37.77 tok/s (+15.1%)**, w4a8 30.95 -> **34.97 tok/s (+13.0%)**, mxfp4 27.08 ->
> **29.72 tok/s (+9.7%)** -- w4a16/w4a8 beat this section's "+4 to +5 tok/s realistic" estimate,
> mxfp4 landed a bit under it (consistent with §2.4's mxfp4-specific small-M GEMM knee -- not
> investigated further). Full per-layout `--mtp 0`/`--mtp 3`/prefill/VRAM table: `docs/perf.md`'s
> "R1 pass" update. **R4 (GEMM tuning for these three shapes) done alongside it**: all three swept
> across all four layouts x seven M-bands (`tools/profile/tune_gemm.py --shapes
> gdn.in_proj_z,attn.k,attn.v --append`), confirming bandwidth-bound-to-M=64 as predicted (e.g.
> `attn.k` w4a8: 5.99us at M=1 vs 6.79us at M=16). **R14/Q13 also landed in this pass** (see below).

---

## 5. Open questions and the experiments that settle them

| # | Question | Experiment | Cost |
|---|---|---|---|
| **Q1** | Is the bf16 layout being WDDM-paged over PCIe? (§2.1) | **Cheapest decisive test first**: read Windows' "Shared GPU memory" counter (Task Manager / `rocm-smi`) and `hipMemGetInfo` immediately after a bf16 load -- if 47.73 GiB was ever nominally allocated on a 31.86 GiB card, paging is confirmed without timing anything. Only if that is inconclusive, time an in-model GEMM (and note [TUNE]'s 564.09 us is not a trustworthy baseline, Q5). | S |
| **Q2** | **Why is the profiled step 5.4 +/- 0.5 ms longer than the steady-state step in every layout?** (§2.5) | R5: profile a steady-state token (not the first generated one), report `gpu_sum + host_enqueue` against `1/tok_s`, and difference the hipEvent instrumentation in/out. **This is the single largest unexplained number in the document** -- bigger than every roadmap item except R1. | S |
| **Q3** | What is the per-kernel breakdown of GDN's 88 us/layer excess? (§2.5) | R5. Expect `recurrent_update` to dominate; if it does not, R6's design changes. | S |
| **Q4** | Is `QuantActFp8Kernel` really ~55 us/call at decode? (§2.5) | Bench it standalone at M=1, K in {5120, 17408}, against `r4d_quant_act_i8` and `r4dx_model_cast_bf16_to_f16`. Compare against the 27/35/55 us upper bounds derived from the MLP block. | S |
| **Q5** | How much does [TUNE] overstate small-shape GEMM performance via MALL residency? (P5; mxfp4 `mlp.down` implies 937 GB/s, `in_proj_qkv` 915 GB/s -- both above DRAM peak) | Re-run `tune_gemm.py` against a rotating ring of >= 4 distinct weight buffers totalling > 256 MiB so nothing is reused. Compare the M=1 rows. **Must land before R12 and before any [TUNE]-based ranking of mxfp4 vs w4a16 is trusted.** | S |
| **Q6** | How much of the 25% gap between the measured 286 TOPS (C19) and the 383 TOPS dense peak (C37) is contention vs real? (The old framing, "which is real, 286 or 407", is settled: 407 exceeds the hardware's dense peak and cannot be device throughput.) | Re-run `tools/bench/wmma_probe_*.exe` with no contending process on device 1, with `rocm-smi` clock sampling (Q8). Sets P9's expected TOPS. | S |
| **Q7** | What is prefill's per-op profile? Is the GEMM share really ~36%? (§2.6) | R5's prefill extension. **This gates R10 vs R11 ordering and is the single most decision-relevant missing measurement about prefill.** | S |
| **Q8** | **Does the card hold boost clock through a decode step?** (C38: [BENCH §4] saw a 2.2x boost ramp inside a microbenchmark; a decode step is ~1200 kernels of 8-150 us with a host sync per step at 300 W TBP) | Sample `rocm-smi` clock / power / GPU-busy at ~100 ms during a steady-state `generate`. If clocks sag, some of the 24% gap §2.5 attributes to kernels is DVFS, and every constant in §1.1 needs a duty-cycle caveat. **Do this before attributing the gap to kernel quality.** | S |
| **Q9** | At what verify width `w` does GDN's `chunk_scan` path beat `w` sequential `recurrent_update` calls? (§2.4, P10) | Bench both via `r4d.pyd` at w in {1,2,4,8,16}. Predicted crossover w* ~= 2-3, because `recurrent_update` re-reads the 3.146 MB state per row (302w MB/token) while `chunk_scan` reads it once. At w=16 that is 4.83 GB vs 0.302 GB -- an 7.5 ms/step difference. | S |
| **Q10** | Two coupled questions for wide speculation: (a) does `r4d_attn_decode_h256_gqa6` cap at `q_len * gqa <= 64` (i.e. `q_len <= 10`)? (b) can the MTP head emit `w` draft rows in **one** pass, or is it `w` sequential full-vocab `lm_head` evaluations? (§2.2) | (a) read the kernel's launch constraints and test at q_len = 8, 10, 12. (b) read the MTP head's definition in the checkpoint. **(b) decides whether R9 exists at all**; (a) decides whether R9 needs two decode calls or the prefill attention kernel. | S |
| **Q11** | Are int4 *activations* numerically acceptable for this model? (gates the C21 `iu4` K=32 path) | Only after R10 exists and is measured compute-bound. Offline: quantize activations to int4 in the reference `transformers` path and measure per-layer rel-L2 against the current ~7-13% quantized-GEMM error. | M |
| **Q12** | Is 7.83 TB/s the real LDS ceiling, or a contention/clock-ramp artifact? (C12 is 42 B/clk/CU against a typical ~128 B/clk design point, and C38 shows its run1 at half of runs 2-3) | Re-run `lds_bandwidth.exe` uncontended with a conflict-free stride sweep and clock sampling. **Directly sets P6/P9's minimum register-reuse factor** (2.3x required at 7.83 TB/s; ~1x if the real figure is ~24 TB/s). | S |
| **Q13** | Where does the 15.75 GiB actually go, and why is it identical for w4a8 (whose weights are 0.35 GiB smaller)? (§2.1) | One `hipMemGetInfo` + per-tensor accounting dump at end of load, per layout, at two `--max-ctx` values. Carried by R13. | S |
| **Q14** | **Does quantizing `gdn.in_proj_z` and `attn.k`/`v` hold accuracy?** (gates R1, the largest decode item) | Offline in the reference `transformers` path: quantize each of the three to w4a8 g128 and measure per-layer rel-L2 and end-to-end perplexity against bf16. `in_proj_z` feeds a sigmoid/silu gate and `k`/`v` feed an already-fp8 KV cache, so the prior is "probably fine, verify per tensor"; `in_proj_a/b` feed the decay and stay bf16 regardless. | M |
| **Q15** | Do GDN-state and activation **stores** allocate in the 64 MiB memory-side MALL, and does gfx12 expose a useful store temporal hint? (P5; the step writes 151 MB of state through it) | Microbench: time `r4d_gdn_recurrent_update`-shaped write traffic with and without any available NT-store qualifier; inspect gfx12 store instruction temporal bits in the ISA doc. Feeds R6's kernel design. | S |
| **Q16** | Does a captured decode graph reproduce logits bit-for-bit, and what does sampling look like when it cannot leave the graph? (P3) | Capture, replay, compare `logits_dev_` against the non-graph path for 32 steps. Any divergence means an arena address or control array is not stable. Separately: design device-side greedy **and** non-greedy sampling, plus the device-side accept/reject for R8. | S |
| **Q17** | Does the §2.3 long-context curve hold? Is 29.1 tok/s at 131k real? | R13. | S |

**Answered by the R1 pass (2026-09-20)**: **Q14** -- yes, quantizing `gdn.in_proj_z` and `attn.k`/
`v` holds accuracy. Rather than the offline `transformers`-side rel-L2/perplexity experiment
originally specified, this was answered with the repo's existing rung-3 golden tests
(`tests/model/test_gdn_layer.cpp`, `tests/model/attention/test_attn_layer.cpp` -- real weights,
real `transformers` activations) extended to load the newly-quantized tensors at the layout under
test: quantizing `attn.k`/`v` raised the attention layer's own rel-err by ~30-40% relative (e.g.
w4a16 decode 6.57e-2 -> 8.83e-2); quantizing `gdn.in_proj_z` did not move the GDN block's error
outside its pre-existing ~7-8e-2 range at all. Neither crossed the task's ">2x worse" bar, so all
three ship quantized in every layout (docs/status.md's R1 section has the full table). **Q13** --
partially answered, not fully closed: `Container::Load`/`Model::Load` now print a real
`hipMemGetInfo`-based `weights/kv+gdn_state/arena+scratch/free` breakdown at the end of every load
(carried by R13/R14). Measured against the real container, `weights` reads identically 13.14 GiB
for w4a16/w4a8/mxfp4 at `--mtp 0` -- the original "why is w4a8 not 0.35 GiB smaller" puzzle
reproduces even with the new breakdown, so a genuine per-tensor accounting dump (not just a
phase-boundary `hipMemGetInfo` delta) is still open. A related, unplanned finding while building
R14's over-commit warning: `hipMemGetInfo` does not report a negative/over-committed free value on
this WDDM stack -- it clamps at ~0 -- so a warning that only compares "bytes consumed vs. free
before" never fires for the real bf16-over-commit case; the shipped warning also checks "free
after is near-zero starting from meaningfully more before", confirmed firing correctly against the
old container's bf16 layout (`docs/perf.md`'s R1 update has the exact message).

**Explicitly resolved by this pass** (previously open in the research docs): hipGraph *does* work on
this Windows HIP 7.15 stack (C32, [BENCH §3b] vs [ARCH §4]); the 16x16x32 int8 WMMA the original
brief assumed does **not** exist -- the int8 shape is 16x16x16, and 16x16x32 exists only for `iu4`
(C19/C21, [LESSONS §1.1]); [LESSONS §4]'s unexplained bf16 per-launch anomaly now has a candidate
mechanism (Q1); AMD's "191 TFLOPS FP16" figure is **dense** (C37), since the same source table lists
383 as its sparse counterpart, which retires [BENCH]'s own open question and shows the measured
156.3 TF/s to be 82% of dense, not above any dense peak; and the VRAM footprint is now fully
accounted for (§2.1).

---

## Revision notes

Changes in this revision, and why (each replaces something the previous revision asserted):

1. **Byte accounting rebuilt against [FMT].** `gdn.in_proj_z`, `gdn.in_proj_a/b`, `gdn.conv1d`,
   `attn.k`, `attn.v` and all norms are **bf16-only, in every layout**. Weights are **16.116 GB
   (15.01 GiB)**, not 13.613 GB (12.68 GiB); the token is **16.506 GB**, not 14.00 GB. The 0.53125
   B/param constant now applies only to the seven quantized linears, and w4a8 is 0.515625 B/param.
2. **VRAM reconciled.** 15.01 GiB of weights + 0.74 GiB of KV/arena/runtime = the measured 15.75 GiB
   (C41); cross-validated by the 2.04 GiB drop measured when `attn.qg/o` were quantized (predicted
   2.07). The previous ~3 GiB of unexplained footprint was the bf16 tensors.
3. **Decode ceiling corrected from "43-46 tok/s" to 36.6 tok/s** (604 GB/s measured, corrected
   bytes), with 38.8 as an unreachable spec bound. C8's 641-649 GB/s is removed from every ceiling
   derivation and re-labelled as an unverified, cache-flattered [TUNE] figure ([BENCH §5]'s own
   caveat). **w4a16 is at 89.6% of its roofline, not 75.6%** -- kernel work on this card is capped
   at +12%.
4. **§2.5 no longer rescales percentages onto the wrong step time.** Both clocks are stated, the
   per-block table uses [PERF]'s unmodified ms, and the constant +5.4 ms offset between the profiled
   and steady-state steps is promoted to Q2. Gap split is now **GDN 46% / MLP 38% / attention 13%**,
   not 80/7/15; "the MLP is done at 97% of roofline" is withdrawn (it is at 81%).
5. **[TUNE] is no longer used as an in-model cost model anywhere** (§2.5's per-layout remainder
   table, §2.6's 523 us/token). The mxfp4 deficit is restated as two candidate causes (scalar fp8
   quant kernel; ~1.5x cache flattery on mxfp4 `down`/`qkv`), separated by Q4/Q5, and the per-quant-
   call cost is re-derived from the MLP block's byte roofline instead.
6. **New top roadmap item R1**: quantize `in_proj_z` + `k`/`v` -- 2.464 GB/token, the only item that
   raises the ceiling (36.6 -> 43.0 tok/s). Gated by new accuracy question Q14.
7. **Launch census re-derived from `linear.cpp` / `gdn_layer.cpp`**: 433 GEMMs (257 quantized + 176
   bf16), **257** quant/cast launches (not 224), ~510 other, ~1200 total. P2's
   "quantize-once-per-producer-row" idea is **withdrawn**: no duplicate quantization exists, because
   every linear that shares a normed row with another is bf16.
8. **Speculation is now priced on both sides** (§2.2, new P10): a sequential width-`w` draft costs
   `w` x 926 MB (1.53 ms), which kills sequential DFlash2 and demands a one-pass or reduced-vocab
   drafter (R9); GDN state rollback is budgeted at 0.50 ms/step; the conv-buffer depth constraint is
   restated; accept/reject must be device-side.
9. **WMMA rates anchored to documented dense peaks (new C37)**: measured 156/162/284/286/593 are
   74-85% of peak; libr4d's 207/412/407/830 exceed dense peak by 1.06-1.08x and are dropped from
   every budget. P9 now targets 170-215 TOPS = 44-56% of the 383 TOPS peak. Q6 rewritten.
10. **P5 re-based**: the "NT protects cache residency for KV/GDN state" justification is deleted (no
    intra-step reuse exists; 16.5 GB streams through a 64 MiB MALL between touches), as is the GDN
    kernel-ordering bullet. NT is now a per-(shape, M) tuning entry (R12), justified by libr4d's
    measured M-dependent behaviour. MALL *write* behaviour added as Q15.
11. **P9 tiling made self-consistent**: 128x256 block with 2x4 waves of 4x4 fragments (was 128x128
    with the same waves, which is arithmetically impossible), LDS recomputed (48 KiB, or 32 KiB with
    raw-4-bit B staging -- the new default), reuse requirement stated as the >=2.3x the arithmetic
    yields with 4x as the design margin.
12. **P3 completed**: the per-token 10 KiB pageable embedding upload (new C39) and the argmax D2H
    (C33) are named as the graph-hostile nodes; moving `text.embed_tokens` to VRAM (2.54 GB, still
    fits at 131k) is the recommended fix; non-greedy sampling is flagged as undesigned.
13. **R3/R7 re-priced against the GPU, not the host**: `host_enqueue` is never the critical path
    (C35, `finish_wait` at 93.5% of wall), so R3 is -0.15..-0.5 ms and R7 is -0.59 ms, matching
    [BENCH §3b]'s independent "~1% of a decode step". R6 re-priced down to -0.5..-1.5 ms.
14. **§2.6 conclusion inverted to match its own decomposition**: a bigger chunk lowers only the
    417 us/token streaming floor, the attention-prefill term grows with `T`, and ~64% of prefill is
    unprofiled -- so R11 outranks R10 and the "biggest prefill lever is a larger chunk" claim is
    withdrawn.
15. **§2.4's operational claim scoped**: `M* >= 16` holds for w4a8/w4a16; mxfp4's `down`/`in_proj_qkv`
    knees at ~8-12 rows make it the wrong verify-path layout (an additional argument for P1).
16. **Clock/power added as a first-class caveat** (new C38, Q8): [BENCH §4]'s 2.2x boost ramp means
    every saturating-kernel constant here may not hold for a chain of 8-150 us kernels at 300 W TBP;
    sample `rocm-smi` before blaming kernels for the roofline gap.
17. **New operational PCIe/IO facts** (C39, C40): 10 KiB/token embedding upload, 87.79 GiB container,
    15-17 s load (60 s bf16) -- the user-visible cost of P1's layout choice.
18. **New cheap item R4**: the bf16 `in_proj_z`/`k`/`v` GEMMs (3.4 GB/token, 20.6% of the step) run
    on an untuned hardcoded config (`gdn_layer.cpp:59`) and a validity-only heuristic
    (`attention/linear.hpp:30-41`); `tune_gemm.py` has never swept those shapes.
