# CLI text generation entry point. Placeholder until filled in.
add_library(r4dx_cli INTERFACE)
target_include_directories(r4dx_cli INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})

# The `r4dx` executable target is added here once src/cli/main.cpp exists.
